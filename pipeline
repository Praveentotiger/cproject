pipeline {
  agent any
  stages {
    stage('build') {
      parallel {
        stage('build1') { steps { /*...*/ } }
        stage('build2') { steps { /*...*/ } }
      }
    }
    stage('test') {
      steps { /* test steps */ }
    }
    stage('deploy') {
      steps { /* deploy steps */ }
    }
  }
}
